import React from "react";
import spin from "../../img/spinner1.gif";
const Spinner = () => {
  return (
    <div>
      <img src={spin} style={{ margin: "auto", display: "flex" }} />
    </div>
  );
};

export default Spinner;
